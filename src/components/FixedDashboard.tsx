// FixedDashboard.tsx — PLACEHOLDER, owned by Dev B. Replace the body; keep path, default export, and props.
import type { Item } from "@/lib/viewspec";
export default function FixedDashboard(_: { items: Item[] }) { return null; }
