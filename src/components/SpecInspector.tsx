// SpecInspector.tsx — PLACEHOLDER, owned by Dev B. Replace the body; keep path, default export, and props.
export default function SpecInspector(_: { spec: unknown; meta: { provider: string; latencyMs: number; source: string }; open: boolean; onClose: () => void }) { return null; }
