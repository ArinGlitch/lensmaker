// AblationToggle.tsx — PLACEHOLDER, owned by Dev B. Replace the body; keep path, default export, and props.
export default function AblationToggle(_: { enabled: boolean; onChange: (v: boolean) => void }) { return null; }
