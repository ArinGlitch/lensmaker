// IntentBar.tsx — PLACEHOLDER, owned by Dev B. Replace the body; keep path, default export, and props.
export default function IntentBar(_: { onSubmit: (intent: string) => void; isLoading: boolean }) { return null; }
