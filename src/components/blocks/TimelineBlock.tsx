// blocks/TimelineBlock.tsx — PLACEHOLDER, owned by Dev B. Replace the body; keep path, default export, and props.
import type { TimelineBlock, Item } from "@/lib/viewspec";
export default function TimelineBlockView(_: { block: TimelineBlock; items: Item[] }) { return null; }
