// blocks/StatBlock.tsx — PLACEHOLDER, owned by Dev B. Replace the body; keep path, default export, and props.
import type { StatBlock, Item } from "@/lib/viewspec";
export default function StatBlockView(_: { block: StatBlock; items: Item[] }) { return null; }
