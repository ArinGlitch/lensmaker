// blocks/ListBlock.tsx — PLACEHOLDER, owned by Dev B. Replace the body; keep path, default export, and props.
import type { ListBlock, Item } from "@/lib/viewspec";
export default function ListBlockView(_: { block: ListBlock; items: Item[] }) { return null; }
