// blocks/TableBlock.tsx — PLACEHOLDER, owned by Dev B. Replace the body; keep path, default export, and props.
import type { TableBlock, Item } from "@/lib/viewspec";
export default function TableBlockView(_: { block: TableBlock; items: Item[] }) { return null; }
