// blocks/CalloutBlock.tsx — PLACEHOLDER, owned by Dev B. Replace the body; keep path, default export, and props.
import type { CalloutBlock, Item } from "@/lib/viewspec";
export default function CalloutBlockView(_: { block: CalloutBlock; items: Item[] }) { return null; }
